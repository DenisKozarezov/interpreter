let f = fn(i) { i + 2 };

// We create an array with different types
let arr = ["123", 123, -123, true];
let item = arr[f(0)]

// All elements in array are not zero-integer, so we should never get "false" in result
if (item) {
    true;
} else {
    false;
}